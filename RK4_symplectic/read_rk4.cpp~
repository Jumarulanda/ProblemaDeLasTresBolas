#include <vector>
#include <iostream>
#include <cmath>

using namespace std;

vector <double> f(vector <double>);

int main()
{
  //Calling rk4
  vector < vector <double> > d {{1,2},{2,3},{2,3,5,2}};
  cout << d[2].size() <<endl;
  return 0;
}

vector <double> f(vector <double> z)
{
  double k = 1;
  double m = 1;

  double xpoint = z[1]/m;
  double ppoint = -k*z[0];
  vector <double> variables {xpoint,ppoint};
  return variables;
  
}
